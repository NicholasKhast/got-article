
<div class="wrap">
    <div class="floatleft"><div id="in" class="col-md-12">
  <header>
    <div class="content text-center wow distance_from_top">
      
      <%= gravatar_for @user, size: 120 %>
      <h1><%= @user.username %></h1>
      <h4 align="center">Welcome to <%= @user.username %>'s page</h4>
      <p>
        <%=link_to "Articles", articles_path, {:class => "btn btn-outline btn-artp btn-lg"} %>
      </p>
    </div>
  </header>
</div></div>
    <div class="floatright"><%= render 'articles/article', obj: @user_articles %>
<div class="center">
  <%= will_paginate @user_articles %>
</div></div>
</div>

